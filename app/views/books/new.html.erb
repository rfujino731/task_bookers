<h1>new book</h1>

<%= form_for(@book, url: '/book') do |f| %>

	<h4>title</h4>
	<%= f.text_field :title %>

	<h4>本文</h4>
	<%= f.text_field :body %>

	<%= f.submit 'Create Book' %>
<% end %>